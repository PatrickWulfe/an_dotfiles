# Agent Shell Transcript

**Agent:** Kiro
**Started:** 2026-06-02 11:36:56
**Working Directory:** /Users/wulfep/.kiro/steering/

---

## User (2026-06-02 11:36:56)

in the dart-flutter.md file, how much of it is already covered by my other steering files?



### Tool Call [completed]: Reading dart-flutter.md:1

**Tool:** read
**Timestamp:** 2026-06-02 11:37:02
**Parameters:**
__tool_use_purpose: Reading the dart-flutter.md file to analyze its contents against other steering files
operations: [((mode . "Line") (path . "/Users/wulfep/.kiro/steering/dart-flutter.md"))]

```

```

## Agent (2026-06-02 11:37:05)

Here's the overlap analysis:

## Heavily Covered by Other Files

**`effective-dart.md`** covers:
- Naming conventions (`lowerCamelCase`, `UpperCamelCase`)
- `final` over `var`
- `const` constructors
- Types/type annotations
- Documentation (`///` doc comments)
- Performance (`const`, avoid expensive build methods)

**`bloc.md`** covers:
- BLoC/Cubit as default state management
- Keep business logic out of widgets
- `context.read<T>()` / `context.watch<T>()` / `BlocBuilder`
- Testing with `bloc_test`, `blocTest`, state transitions
- Flutter Bloc widgets (`BlocBuilder`, `BlocListener`, etc.)

**`project-context.md`** covers:
- Melos monorepo structure
- PRs can only touch one area at a time
- Alpha-sort parameters
- Code formatting (80 char lines, `dart format`)

**`widget-mapping-gather-ui.md`** covers:
- Localization via `context.l10n` / ARB files
- No hardcoded strings
- Private widget classes, not methods returning widgets
- Feature-first folder structure

## Unique to `dart-flutter.md` (not covered elsewhere)

- `ignore_for_file` prohibition with justification rule
- Single quotes preference
- Trailing commas rule
- Widget Design section details (composition over inheritance, `Key` params, flatten nested trees, prefer `StatelessWidget`)
- Full Architecture & Structure details (subfeatures max 1 deep, barrel files, repository/subrepository nesting rules, `RepositoryProvider`/`BlocProvider` DI)
- Null Safety section (pattern matching over null checks, `sealed` classes, `required` named params)
- Error Handling (`Result` types, `ValueWrapper`, `Either`)
- Testing details beyond bloc (widget test helpers like `pumpTestableWidget`, `pumpRoute`; mocktail patterns; `Fake` classes; fallback values; `pumpEventQueue`; test directory mirroring)
- Performance specifics (`BlocSelector` with `buildWhen`, `ListView.builder`)
- Packages & Dependencies (pin versions, minimal deps)
- Accessibility (`Semantics` widgets)

## Summary

Roughly **40–50%** of `dart-flutter.md` is already covered by your other steering files. The unique value it still provides is:

1. **Architecture specifics** (subfeatures, barrel files, repository nesting rules)
2. **Widget design philosophy** (composition, flattening, Keys)
3. **Testing patterns** beyond blocs (widget test helpers, mocktail conventions, fakes)
4. **Null safety / pattern matching** guidance
5. **Error handling patterns** (Result/Either)
6. **Dart-specific style** (trailing commas, single quotes, `ignore_for_file` ban)

If you wanted to slim it down, you could remove the sections on naming, state management basics, and localization since those are well-covered elsewhere. The architecture, widget design, testing details, and null safety sections are where this file adds the most distinct value.

## User (2026-06-02 11:37:44)

remove the parts that are already covered please



### Tool Call [completed]: Creating dart-flutter.md

**Tool:** edit
**Timestamp:** 2026-06-02 11:38:50
**Command:** create
**Parameters:**
__tool_use_purpose: Removing redundant sections from dart-flutter.md that are already covered by other steering files
path: /Users/wulfep/.kiro/steering/dart-flutter.md
content: ---
inclusion: fileMatch
fileMatchPattern: "**/*.dart,**/pubspec.yaml"
---

# Dart & Flutter Coding Standards

## Code Style & Conventions
- Prefer single quotes for strings (`'hello'`), unless the string contains a single quote.
- Use trailing commas for multi-line argument lists (improves formatting and diffs).
- NEVER use `ignore_for_file` tags. Keep ignore tags specific to the code they're affecting and add a comment on why the ignore tag was necessary. Ignore tags should only be used when absolutely necessary.

## Widget Design
- Prefer **composition over inheritance** — build small, reusable widgets.
- Avoid deeply nested widget trees — flatten by extracting into dedicated widget classes.
- Prefer `StatelessWidget` unless local mutable state is required.
- Use `Key` parameters when widget identity matters (e.g., lists, animations).

## Architecture & Structure
- Follow a **feature-first** folder structure:
  - Each feature lives in its own directory under `lib/` (e.g., `lib/home/`, `lib/vehicle_profile/`).
  - Features contain: `bloc/`, `view/`, `widgets/`, and optionally `extensions/`, `models/`.
  - **Subfeatures** are allowed with a **maximum of 1 level deep** via a `subfeatures/` directory (e.g., `lib/home/subfeatures/onboarding_checklist/`). Do not nest subfeatures further.
  - Export feature contents via a barrel file named after the feature (e.g., `home.dart`).
- **Repositories** live in `packages/` as separate Dart packages (e.g., `packages/user_repository/`).
  - Repositories may contain **subrepositories** for related domain logic (e.g., `user_repository/lib/src/subrepositories/user_gatherings_subrepository.dart`).
  - Subrepositories are **maximum 1 level deep** — do not nest subrepositories within subrepositories.
- Separate concerns: UI (widgets/views), logic (blocs/cubits), data (repositories/subrepositories), models.
- Use **dependency injection** via `RepositoryProvider` and `BlocProvider` from `flutter_bloc`.
- Keep `main.dart` minimal — delegate setup to dedicated bootstrap/configuration files.

## Null Safety & Types
- Embrace **sound null safety** — avoid `!` unless you're certain; prefer null checks or `??`.
- **Prefer pattern matching over null check operators** — use `if (value case final v?)` or `switch` expressions instead of `value != null` or `value!`.
- Prefer `required` named parameters for clarity.
- Use `sealed` classes (Dart 3+) for exhaustive pattern matching.

## Error Handling
- Use `try/catch` with specific exception types.
- Prefer `Result` types, `ValueWrapper` with status enums, or `Either` (from `fpdart` or `dartz`) for expected failures.
- Avoid swallowing exceptions silently — log or propagate meaningfully.

## Testing
- Mirror the `lib/` folder structure in `test/` — each feature has a corresponding test directory.
- Separate test files for bloc, event, and state (e.g., `home_bloc_test.dart`, `home_event_test.dart`, `home_state_test.dart`).
- **Widget tests:**
  - Use `WidgetTester` extensions (e.g., `pumpTestableWidget`, `pumpRoute`, `pumpTestableWidgetWithOverriddenInnerRouteWidget`) to pump widgets with required providers and routing context.
  - Provide mock repositories and blocs via the test helper parameters.
- **Mocking:**
  - Use `mocktail` for mocking dependencies.
  - Create mock classes prefixed with `Mock` (e.g., `MockUserRepository`) or `_Mock` for file-private mocks.
  - Use `Fake` classes for simple stubs that don't need verification.
  - Register fallback values in `setUpAll` for custom types used with `any()`.
- **Test helpers:**
  - Centralize reusable test utilities in `test/helpers/` (e.g., `pump_app.dart`, `mocks/`, `fakes/`, `fallback_values/`).
  - Use `pumpEventQueue()` to allow async bloc events to complete.
- **Assertions:**
  - Use `expect()` with matchers like `isA<T>()`, `equals()`, `.having()` for granular assertions.
  - Use `verify()` and `verifyNever()` to assert mock interactions.
  - Aim for high coverage on critical paths (blocs, repositories); don't chase 100% blindly.

## Performance
- Avoid rebuilding large widget subtrees — use `Selector`, `BlocSelector`, or `BlocBuilder` with `buildWhen`.
- Use `ListView.builder` / `GridView.builder` for long lists.
- Profile with DevTools before optimizing prematurely.

## Packages & Dependencies
- Prefer well-maintained, popular packages from pub.dev.
- Pin versions in `pubspec.yaml` for reproducibility.
- Keep dependencies minimal — avoid bloat.

## Accessibility
- Provide `Semantics` widgets for accessibility.


```
╭─────────╮
│ changes │
╰─────────╯

@@ -5,40 +5,17 @@
 
 # Dart & Flutter Coding Standards
 
-## Purpose
-Specialized guidance for writing, refactoring, debugging, and improving Dart/Flutter code. Apply Flutter best practices, idiomatic Dart patterns, and modern architecture conventions by default.
-
 ## Code Style & Conventions
-- Follow Effective Dart guidelines.
-- Use `lowerCamelCase` for variables/functions, `UpperCamelCase` for classes/types.
-- Prefer `final` over `var` when values don't change.
-- Use `const` constructors wherever possible for widgets.
 - Prefer single quotes for strings (`'hello'`), unless the string contains a single quote.
-- Keep lines under 80 characters when practical.
 - Use trailing commas for multi-line argument lists (improves formatting and diffs).
-- Alphabetize function and method parameters.
-- Avoid hardcoded UI strings in favor of using localizations.
 - NEVER use `ignore_for_file` tags. Keep ignore tags specific to the code they're affecting and add a comment on why the ignore tag was necessary. Ignore tags should only be used when absolutely necessary.
 
-## Project Constraints
-- This is a Melos monorepo with three independent areas: `gather_app`, `gather_ui`, and `gather_api`.
-- **PRs can only touch one area at a time** — cannot merge a PR with changes spanning multiple areas (e.g., changes in both `gather_app` and `gather_ui` must be separate PRs).
-- If a change requires updates to `gather_ui` or `gather_api`, those must be done in a separate PR first.
-
 ## Widget Design
 - Prefer **composition over inheritance** — build small, reusable widgets.
-- **Prefer `StatelessWidget` or `StatefulWidget` class declarations over helper methods that return `Widget` objects.** Extract widget subtrees into separate widget classes, not private `Widget _buildSomething()` methods.
-- Use `const` widgets to optimize rebuilds.
 - Avoid deeply nested widget trees — flatten by extracting into dedicated widget classes.
 - Prefer `StatelessWidget` unless local mutable state is required.
 - Use `Key` parameters when widget identity matters (e.g., lists, animations).
 
-## State Management
-- Default to **BLoC/Cubit** (with `flutter_bloc`) unless the project uses another pattern.
-- Keep business logic out of widgets — delegate to blocs, cubits, or repositories.
-- Avoid `setState` for anything beyond trivial local UI state.
-- Use `context.read<T>()` for actions, `context.watch<T>()` or `BlocBuilder` for reactive UI.
-
 ## Architecture & Structure
 - Follow a **feature-first** folder structure:
   - Each feature lives in its own directory under `lib/` (e.g., `lib/home/`, `lib/vehicle_profile/`).
@@ -51,12 +28,10 @@
 - Separate concerns: UI (widgets/views), logic (blocs/cubits), data (repositories/subrepositories), models.
 - Use **dependency injection** via `RepositoryProvider` and `BlocProvider` from `flutter_bloc`.
 - Keep `main.dart` minimal — delegate setup to dedicated bootstrap/configuration files.
-- Use **melos** for monorepo management.
 
 ## Null Safety & Types
 - Embrace **sound null safety** — avoid `!` unless you're certain; prefer null checks or `??`.
 - **Prefer pattern matching over null check operators** — use `if (value case final v?)` or `switch` expressions instead of `value != null` or `value!`.
-- Use explicit types for public APIs; type inference is fine for local variables.
 - Prefer `required` named parameters for clarity.
 - Use `sealed` classes (Dart 3+) for exhaustive pattern matching.
 
@@ -67,10 +42,7 @@
 
 ## Testing
 - Mirror the `lib/` folder structure in `test/` — each feature has a corresponding test directory.
-- **Unit tests for blocs/cubits:**
-  - Use `bloc_test` package with `blocTest` or `blocTestWithClock` for time-dependent logic.
-  - Test events, state transitions, and side effects.
-  - Separate test files for bloc, event, and state (e.g., `home_bloc_test.dart`, `home_event_test.dart`, `home_state_test.dart`).
+- Separate test files for bloc, event, and state (e.g., `home_bloc_test.dart`, `home_event_test.dart`, `home_state_test.dart`).
 - **Widget tests:**
   - Use `WidgetTester` extensions (e.g., `pumpTestableWidget`, `pumpRoute`, `pumpTestableWidgetWithOverriddenInnerRouteWidget`) to pump widgets with required providers and routing context.
   - Provide mock repositories and blocs via the test helper parameters.
@@ -88,7 +60,6 @@
   - Aim for high coverage on critical paths (blocs, repositories); don't chase 100% blindly.
 
 ## Performance
-- Use `const` constructors and widgets.
 - Avoid rebuilding large widget subtrees — use `Selector`, `BlocSelector`, or `BlocBuilder` with `buildWhen`.
 - Use `ListView.builder` / `GridView.builder` for long lists.
 - Profile with DevTools before optimizing prematurely.
@@ -96,15 +67,7 @@
 ## Packages & Dependencies
 - Prefer well-maintained, popular packages from pub.dev.
 - Pin versions in `pubspec.yaml` for reproducibility.
-- Use **melos** for monorepo management.
 - Keep dependencies minimal — avoid bloat.
 
-## Localization & Accessibility
-- Use `flutter_localizations` and ARB files for i18n.
+## Accessibility
 - Provide `Semantics` widgets for accessibility.
-- Avoid hardcoded strings in UI — use localization keys.
-
-## Documentation
-- Add `///` doc comments to public APIs.
-- Use `// TODO:` or `// FIXME:` for pending work.
-- Keep README and CHANGELOG updated for packages.
```

## Agent (2026-06-02 11:38:57)

Done. Removed:

- **Naming conventions** (covered by `effective-dart.md`)
- **`final`/`const`/types/documentation** (covered by `effective-dart.md`)
- **State Management section** (covered by `bloc.md`)
- **Project Constraints / Melos / monorepo rules** (covered by `project-context.md`)
- **Localization section** (covered by `widget-mapping-gather-ui.md`)
- **Bloc-specific testing** (covered by `bloc.md`)

The file went from ~130 lines to 73, keeping only the unique guidance on widget design, architecture details, null safety, error handling, testing patterns beyond blocs, performance, packages, and accessibility.

